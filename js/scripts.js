// scripts.js

// Ajoutez des interactions JavaScript si nécessaire

// Exemple : Afficher une alerte lorsque l'iframe est cliqué
document.querySelectorAll('.project iframe').forEach(function(iframe) {
    iframe.addEventListener('click', function() {
        alert('Vous avez cliqué sur le tableau de bord Power BI');
    });
});
